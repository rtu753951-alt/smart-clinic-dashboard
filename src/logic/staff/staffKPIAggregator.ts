import { AppointmentRecord, StaffWorkloadRecord } from "../../data/schema";

export function aggregateStaffKPI(
  appointments: AppointmentRecord[],
  staffWorkload: StaffWorkloadRecord[]
) {
  return [];
}